# Audio Generation Dependencies
# For generating professional budgie sounds using Meta's AudioGen

# Core audio generation
# Note: audiocraft has specific torch version requirements, install it first
audiocraft

# Audio processing
soundfile>=0.12.0
pydub>=0.25.0
ffmpeg-python>=0.2.0

# CLAP semantic refinement (optional, for highest quality)
msclap

# Audio analysis (optional, for quality checking)
librosa>=0.10.0
matplotlib>=3.7.0
pyloudnorm>=0.1.0

# Note: Also requires ffmpeg installed via Homebrew:
# brew install ffmpeg
